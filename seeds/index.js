const categorySeeds = require('./category-seeds');
const productSeeds = require('./product-seeds');
const tagSeeds = require('./tag-seeds');
const productTagSeeds = require('./product-tag-seeds');
const {categoryData, productData, productTagData, tagData} = require('../seeds');
// const {Category, Product, ProductTag, Tag} = require('../model')

// const seedData = async  () => {
//     await Category.bulkCreate(categorySeeds)
    
// }
 
// seedData;